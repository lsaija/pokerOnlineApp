import { IsNoAdminDirective } from './is-no-admin.directive';

describe('IsNoAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new IsNoAdminDirective();
    expect(directive).toBeTruthy();
  });
});
