import { DecodificaRuoloPipe } from './decodifica-ruolo.pipe';

describe('DecodificaRuoloPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodificaRuoloPipe();
    expect(pipe).toBeTruthy();
  });
});
